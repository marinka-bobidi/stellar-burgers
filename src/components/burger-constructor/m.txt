initialState:  {
  price: ...,
  ingredients: ...[], 
} --------->
reducers :{ 
  addIngredient (id) = > {
    price += ingredient(id).price
    ingredients.push(id) ??????????????????
  }
  removeIngredient: (id) => {
    ingredients = ingredients.filter(.....(id))
    price = price - ingredient.price
  }
}

selectors:  {
  getUselector: (state) => state
}

 (state, {payload})

 dispatch(action(x1,x2,x3))